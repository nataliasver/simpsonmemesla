{
  "version": 2,
  "builds": [
    {
      "src": "./api/server.js",
      "use": "@now/node"
    }
  ],
  "routes": [
    {
      // Specify which paths will route to a destination using a regex
      "src": "/(.*)",
      // destination
      "dest": "./api/server.js"
    }
  ]
}